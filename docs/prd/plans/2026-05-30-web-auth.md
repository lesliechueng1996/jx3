# Web Auth Implementation Plan

- **PRD**: [2026-05-30-web-auth-design.md](../specs/2026-05-30-web-auth-design.md)
- **Date**: 2026-05-30

## Tasks

- [x] Add shadcn components: sonner, input, card, field, label, separator
- [x] Install `@tanstack/react-query` and `sonner`
- [x] Create `auth-credentials-schema.ts` with zod validation
- [x] Create route-local components under `routes/login/-components/`
- [x] Update `/login` route and `/` index content
- [x] Wire `QueryClientProvider` and `Toaster` in root layout
- [x] Configure Vitest in `vite.config.ts`
- [x] Add unit tests (schema, form, proxy)

## Verification

```bash
bun run --filter @jx3/web test
bun run --filter @jx3/web check
```

Manual: open `/login`, sign up / sign in with email, try GitHub SSO, confirm redirect to `/` showing `index`.
